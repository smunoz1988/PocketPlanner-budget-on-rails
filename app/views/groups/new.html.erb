<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <%= stylesheet_link_tag 'newForm', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>

    <div class="topSection">
      <%= link_to '&larr;'.html_safe, user_groups_path(current_user), class: "backButton" %>
      <h2 class="sectionName">NEW CATEGORY</h2>
      <% if user_signed_in? %>
        <%= button_to "Sign out", destroy_user_session_path, :method => :delete, class: "signOutButton" %>
      <% end %>
    </div>

    <turbo-frame id="flash_messages">
      <%= render 'shared/flash_messages' %>
    </turbo-frame>

    <div class="formContainer">
        <%= form_with model: @group, url: user_groups_path(current_user) do |form| %>

          <div class="field">
            <%= form.text_field :name, placeholder: "Category Name", autofocus: true, class: 'formFields' %>
          </div>

            <div class="field">
              <%= form.label :icon, "Select an Icon", class: "iconCont" %>
                <div class="icon-list">
                  <% ["bycicle.png", "car.png", "customer-service(1).png", "fast-food.png", "grocery.png", "healthcare.png", "house.png", "mechanic.png"].each do |icon_filename| %>
                    <label>
                      <%= radio_button_tag "group[icon]", icon_filename %>
                      <%= image_tag icon_filename, alt: "Icon", width: "32px" %>
                    </label>
                <% end %>
              </div>
            </div>

        
          <%= form.submit 'Save', class: "submitBtn" %>
        <% end %>

        <br />


      </div>

  </body>
</html>